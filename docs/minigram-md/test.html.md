# test.html

> MiniGram MD Intelligence V6

## 1. File Identity

| Property | Value |
|---|---|
| Source | `test.html` |
| Extension | `.html` |
| Bytes | 2654 |
| Lines | 143 |
| SHA-256 | `af653bb557204b21f8267764ca809f5457855d7430d896384cfaee76a680fc4d` |
| Dependency Depth | 1 |

## 2. Project Understanding

Project understanding generated from the local intelligence engine.

## 3. Architecture Context

Architecture derived from the complete local relation graph.

## 4. File Role

This file belongs to:

`test.html`

The local intelligence engine detected
4 direct dependencies
and 1 consumers.

## 5. Dependencies

- `global_css/like.html`
- `global_css/comment.html`
- `global_css/share.html`
- `global_css/svg.html`

## 6. Used By

- `report.json`

## 7. Exact Relation Flow

- `test.html` → `global_css/like.html` **[string-path]**
- `test.html` → `global_css/comment.html` **[string-path]**
- `test.html` → `global_css/share.html` **[string-path]**
- `test.html` → `global_css/svg.html` **[string-path]**

## 8. Local Symbols

- None

## 9. Exports

- None

## 10. Unresolved References

- None

## 11. Error / Problem Detection

- No detected errors

## 12. Project Systems

- None

## 13. Project Risks

- None

## 14. Recommendations

- None

## 15. Execution / Architecture Flow

See the generated relation graph and file-level flows.

---

# ORIGINAL SOURCE CODE

The following is the **exact local source content**
read from:

`/storage/emulated/0/MINIGRAM1/test.html`

It is NOT AI generated or rewritten.

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Pro Icons UI</title>

<style>
  body{
    background:#000;
    display:flex;
    align-items:center;
    justify-content:center;
    height:100vh;
  }

  /* ===== CONTAINER ===== */
  .bar{
    display:flex;
    gap:40px; /* 🔥 balanced spacing */
  }

  /* ===== ICON WRAPPER ===== */
  .icon{
    display:inline-flex;
    align-items:center;
    justify-content:center;
    opacity:0;
    transform:scale(0.85);
    animation:fadeIn 0.35s ease forwards;
  }

  /* ===== GLOBAL SVG SIZE ===== */
  .icon svg{
    width:52px;
    height:52px;
    cursor:pointer;
    transition:transform 0.25s ease, opacity 0.25s ease, filter 0.25s ease;
  }

  /* ===== HOVER ===== */
  .icon:hover svg{
    transform:scale(1.12);
    opacity:0.95;
    filter:drop-shadow(0 0 4px rgba(255,255,255,0.25));
  }

  /* ===== CLICK ===== */
  .icon:active svg{
    transform:scale(0.88);
  }

  /* ===== ACTIVE STATES ===== */

  /* ❤️ LIKE ACTIVE */
  .icon.like.active path{
    fill:#ff3040;
    stroke:#ff3040;
  }

  /* 🔖 SAVE ACTIVE */
  .icon.save.active path{
    fill:white;
    stroke:white;
  }

  /* ===== FADE IN ===== */
  @keyframes fadeIn{
    to{
      opacity:1;
      transform:scale(1);
    }
  }

</style>
</head>

<body>

<div class="bar">

  <!-- ❤️ LIKE -->
  <div class="icon like"></div>

  <!-- 💬 COMMENT -->
  <div class="icon comment"></div>

  <!-- ✈️ SHARE -->
  <div class="icon share"></div>

  <!-- 🔖 SAVE -->
  <div class="icon save"></div>

</div>

<script>
const PATHS = {
  like: "./global_css/like.html",
  comment: "./global_css/comment.html",
  share: "./global_css/share.html",
  save: "./global_css/svg.html"
};

/* ===== CACHE ===== */
const cache = {};

/* ===== LOAD FUNCTION ===== */
function loadIcon(type){
  if(cache[type]){
    inject(type, cache[type]);
    return;
  }

  fetch(PATHS[type])
    .then(res => res.text())
    .then(svg => {
      cache[type] = svg;
      inject(type, svg);
    })
    .catch(err=>{
      console.error(`❌ ${type} load failed:`, err);
    });
}

/* ===== INJECT ===== */
function inject(type, svg){
  document.querySelectorAll("." + type).forEach(el=>{
    el.innerHTML = svg;

    // 🔥 CLICK TOGGLE (LIKE + SAVE)
    if(type === "like" || type === "save"){
      el.addEventListener("click", ()=>{
        el.classList.toggle("active");
      });
    }
  });
}

/* ===== INIT ===== */
Object.keys(PATHS).forEach(loadIcon);
</script>

</body>
</html>
```

---

Generated by MiniGram MD Intelligence V6.
