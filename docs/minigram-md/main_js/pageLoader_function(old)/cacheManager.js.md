# main_js/pageLoader_function(old)/cacheManager.js

> MiniGram MD Intelligence V6

## 1. File Identity

| Property | Value |
|---|---|
| Source | `main_js/pageLoader_function(old)/cacheManager.js` |
| Extension | `.js` |
| Bytes | 710 |
| Lines | 34 |
| SHA-256 | `35f8ff6a38d54407e958d6034bc605a46d56d1632980f6fed7420e8ef5717a9f` |
| Dependency Depth | 0 |

## 2. Project Understanding

Project understanding generated from the local intelligence engine.

## 3. Architecture Context

Architecture derived from the complete local relation graph.

## 4. File Role

This file belongs to:

`main_js`

The local intelligence engine detected
0 direct dependencies
and 0 consumers.

## 5. Dependencies

- None

## 6. Used By

- None

## 7. Exact Relation Flow

- No local relation detected

## 8. Local Symbols

- `LIMIT`
- `keys`
- `removed`
- `i`
- `key`

## 9. Exports

- None

## 10. Unresolved References

- None

## 11. Error / Problem Detection

- **LOW** — Duplicate Filename: cachemanager.js appears in 2 locations

## 12. Project Systems

- None

## 13. Project Risks

- None

## 14. Recommendations

- None

## 15. Execution / Architecture Flow

See the generated relation graph and file-level flows.

---

# ORIGINAL SOURCE CODE

The following is the **exact local source content**
read from:

`/storage/emulated/0/MINIGRAM1/main_js/pageLoader_function(old)/cacheManager.js`

It is NOT AI generated or rewritten.

```javascript
console.log("🚀 CACHE MANAGER READY (PROTECTED)");

window.CACHE_PRIORITY = {
  home: 100 // 🔥 never remove HOME first
};

window.cleanupCache = function() {

  const LIMIT = window.IS_LOW_END ? 2 : 5;

  if (!window.PAGE_CACHE) return;

  while (window.PAGE_CACHE.size > LIMIT) {

    const keys = Array.from(window.PAGE_CACHE.keys());

    let removed = false;

    for (let i = 0; i < keys.length; i++) {

      const key = keys[i];

      // 🔥 SKIP HOME ALWAYS
      if (key === "home") continue;

      window.PAGE_CACHE.delete(key);
      removed = true;
      break;
    }

    // ❌ agar sirf HOME bacha hai, break infinite loop
    if (!removed) break;
  }
};
```

---

Generated by MiniGram MD Intelligence V6.
