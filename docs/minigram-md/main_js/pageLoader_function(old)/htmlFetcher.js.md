# main_js/pageLoader_function(old)/htmlFetcher.js

> MiniGram MD Intelligence V6

## 1. File Identity

| Property | Value |
|---|---|
| Source | `main_js/pageLoader_function(old)/htmlFetcher.js` |
| Extension | `.js` |
| Bytes | 446 |
| Lines | 23 |
| SHA-256 | `810ca369c1416179f7b86127d36fddc2b5f50356ec9ce4f5f7ca0ded555db81a` |
| Dependency Depth | 0 |

## 2. Project Understanding

Project understanding generated from the local intelligence engine.

## 3. Architecture Context

Architecture derived from the complete local relation graph.

## 4. File Role

This file belongs to:

`main_js`

The local intelligence engine detected
0 direct dependencies
and 0 consumers.

## 5. Dependencies

- None

## 6. Used By

- None

## 7. Exact Relation Flow

- No local relation detected

## 8. Local Symbols

- `res`
- `html`

## 9. Exports

- None

## 10. Unresolved References

- None

## 11. Error / Problem Detection

- **LOW** — Duplicate Filename: htmlfetcher.js appears in 2 locations

## 12. Project Systems

- None

## 13. Project Risks

- None

## 14. Recommendations

- None

## 15. Execution / Architecture Flow

See the generated relation graph and file-level flows.

---

# ORIGINAL SOURCE CODE

The following is the **exact local source content**
read from:

`/storage/emulated/0/MINIGRAM1/main_js/pageLoader_function(old)/htmlFetcher.js`

It is NOT AI generated or rewritten.

```javascript
window.fetchHTML = async function(url, options = {}) {

  const { signal } = options;

  if (window.PAGE_HTML_CACHE.has(url)) {
    return window.PAGE_HTML_CACHE.get(url);
  }

  const res = await fetch(
    window.fixPath(url),
    signal ? { signal } : {}
  );

  if (!res.ok) {
    throw new Error(`Fetch failed: ${url}`);
  }

  const html = await res.text();

  window.PAGE_HTML_CACHE.set(url, html);

  return html;
};
```

---

Generated by MiniGram MD Intelligence V6.
